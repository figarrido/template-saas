---
---

Green the operator-MFA E2E on `main`. Test- and fixture-only changes to
`packages/*` (RLS + integration fixtures updated for RFC-valid seed UUIDs), so
no package release is needed — empty changeset by design.
