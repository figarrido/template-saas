---
'@template/auth': minor
'@template/db': patch
---

Org creation slice 1 — first-org tracer bullet.

- Add the shared `createOrganizationSchema` / `CreateOrganizationInput` Zod
  schema (one field: name, 2–50 chars, trimmed) reused by the first-org form
  and the create-organization Server Action.
- Export `invalidInputFirstIssue` so `apps/web` reuses the canonical
  first-issue validation policy.
- Add `create_organization` RPC function type to `database.types.ts`
  (normally auto-generated by `pnpm db:types` after `pnpm db:reset`).
