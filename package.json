{
  "name": "template-saas",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "setup": "tsx scripts/setup.ts",
    "dev": "portless",
    "dev:webhooks": "stripe listen --forward-to https://template.localhost/api/webhooks/billing/stripe",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:rls": "turbo run test:rls",
    "test:e2e": "turbo run test:e2e",
    "db:reset": "supabase db reset",
    "db:types": "turbo run db:types",
    "db:types:check": "turbo run db:types:check",
    "db:introspect": "turbo run db:introspect",
    "db:introspect:check": "turbo run db:introspect:check",
    "env:example": "turbo run env:example",
    "env:local": "tsx scripts/write-env-local.ts",
    "env:example:check": "turbo run env:example:check",
    "jobs:codegen": "turbo run jobs:codegen",
    "jobs:codegen:check": "turbo run jobs:codegen:check",
    "supabase:promote-pr": "tsx scripts/promote-pr.ts",
    "graph": "graphify extract .",
    "sandcastle": "tsx .sandcastle/main.mts",
    "changeset": "changeset",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "^0.12.0",
    "@changesets/cli": "^2.27.0",
    "@standard-schema/spec": "^1.1.0",
    "@stripe/cli": "1.43.6",
    "portless": "^0.15.1",
    "prettier": "^3.9.4",
    "supabase": "2.109.1",
    "tsx": "^4.23.0",
    "turbo": "^2.10.4",
    "typescript": "^5.6.0",
    "zod": "^3.25.76"
  }
}
